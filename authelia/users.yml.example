# =============================================================================
# Authelia Users Database — Single User
# =============================================================================
# 1. Copy this file:  cp users.yml.example users.yml
# 2. Generate a password hash:
#    docker run --rm authelia/authelia:latest \
#      authelia crypto hash generate argon2 --password 'YOUR_PASSWORD'
# 3. Paste the hash below and set your email.
# =============================================================================

users:
  admin:
    disabled: false
    displayname: "Admin"
    email: admin@example.com
    password: "$argon2id$v=19$m=65536,t=3,p=4$REPLACE_WITH_YOUR_GENERATED_HASH"
    groups: []
